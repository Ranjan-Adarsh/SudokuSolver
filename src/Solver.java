//Solver and Generator will use the Grid.
//Generator should throw two things, the Solution and the Grid,
//After that solver will take control.

//Requirments here:
// --After every entry we could see the sudoku(Should we make it an option?)
// --Ofcourse the cells that must be filled should be editable, if cells already in
// question, invoke an error, if not present fill it, if already element present edit it.
// --If user accepts defeat, display the answer.
// --The values entered by user should be validated first and apt error msg dispayed.
// --Solver should have an option to return to main menu.
public class Solver {

}
